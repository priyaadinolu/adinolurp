--- 
# Step 1.1 — apt-get update
- name: update packages 
  apt: 
   update_cache: yes

# Step 1.2 — sudo apt install default-jdk
- name: install java jdk
  apt:
    name: default-jdk
    state: present   
